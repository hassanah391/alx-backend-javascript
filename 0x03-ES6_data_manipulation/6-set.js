/**
 * Converts an array to a set.
 *
 * @param {*Array} array - an array of elements
 * @returns {*Set}- a Set from an array.
 */
export default function setFromArray(array) {
  return new Set(array);
}
